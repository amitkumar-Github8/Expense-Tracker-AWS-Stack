{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Install dependencies",
			"type": "shell",
			"command": "python -m venv .venv && source .venv/Scripts/activate && pip install -r requirements.txt",
			"isBackground": false
		}
	]
}